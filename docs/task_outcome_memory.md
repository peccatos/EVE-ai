# Task Outcome Memory

Task outcomes are written under:

```text
memory/task_outcomes/
```

They summarize the real task loop:

```text
task, plan, proposal, approval, apply, validation, report, PR summary
```

Outcomes include success, failure reason, changed files, patch operation count,
risk level, validation status, and lessons.
